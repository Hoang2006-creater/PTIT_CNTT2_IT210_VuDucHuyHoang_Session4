package com.restaurant.session04.session05;

public class BaoCao {
//    Lỗi ép kiểu xảy ra khi dữ liệu từ URL không đúng kiểu (String → Long), cần xử lý bằng @ExceptionHandler để tránh crash hệ thống.
//    Phương thức DELETE là idempotent nên an toàn khi gọi nhiều lần trong điều kiện mạng không ổn định.
//    Ngược lại, POST không idempotent nên có thể gây trùng dữ liệu nếu người dùng thao tác lặp lại.
//    Vì vậy, cần thiết kế API cẩn thận để đảm bảo tính ổn định và an toàn dữ liệu.
}
